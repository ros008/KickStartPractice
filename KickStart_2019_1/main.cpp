#include <iostream>
#include <string>

using namespace std;

int main() {
	ios::sync_with_stdio(false); cin.tie(NULL);

	int test_cases_num;
	cin >> test_cases_num;

	for (int i = 0; i < test_cases_num; i++) {
		int min_num, max_num;
		cin >> min_num >> max_num;
		
		int num_tries;
		cin >> num_tries;

		while (true) {
			int try_num;
			cin >> try_num;
			if()
		}
	}


	return 0;
}